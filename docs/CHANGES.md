# 变更记录

## 2026-08-30（十三）：多平台各出完整五图 + 平台差异化出图

- **每个平台都生成完整五图**（原策略为首平台全五图、其余平台只出白底主图适配版）：后端流水线平台循环统一使用 `IMAGE_TYPES`，图片调用次数 = 平台数 × 5；每平台白底图各做一次视觉质检；前端 `imageTypesForPlatform` 同步改为每平台五槽位，生成工作台每个平台区都渲染完整五图网格。
- **平台差异化出图**：`platformRule` 重写为「平台 × 图类」20 组中文规则——每平台独有风格基调（TikTok 竖版信息流抓拍感/年轻活力、Temu 性价比直给/细节饱满、Shopee 移动端简洁明快/色彩饱和、Amazon 专业棚拍/干净可信）× 五类图各自的方向与构图要求，多平台图片组互不雷同、贴合各平台自身特色，而非同一套图换平台名。
- 文案与文档同步：创作页提示「每个平台均生成完整五图，按平台规范差异化出图」、调用计划按平台数计次（N×5 次图片 + N 次质检）；生成工作台平台区标签改「完整五图 · 按平台规范差异化」；CLAUDE.md / architecture / integration-guide / 提交内容 / 三个附加材料 HTML 全部对齐新策略。
- 验证：后端 Maven compile、前端 `tsc -b` 通过；重启后端后双平台（Amazon + TikTok Shop）真实 E2E：10 槽位（5+5）全部生成、每平台白底图各质检、十图 URL 全部唯一且两平台提示词按平台规则差异化。

## 2026-08-30（十二）：放大/下载铺开到各工作台 + 发给图片模型的提示词全中文化

- **图片放大/下载覆盖各工作台**：`ImageLightbox` 从 StudioView 抽为共享组件 `ImageLightbox.tsx`；单图工具工作台结果区「当前图 / 新图」双击放大（lightbox 内可下载原图）、AI 详情页工作台「03 配图引用」缩略图双击放大、详情页模块配图（`DetailPageView`，生成工作台与详情页工作台共用）双击放大。生成工作台槽位行为不变（双击放大 + 右上角下载）。
- **提示词全中文**（发给图片模型的模板不再中英混排，按用户要求）：后端五图生成模板 `fallbackPrompt`、平台合规规则 `platformRule`、参考图一致性句 `buildPrompt`、本地化兜底指令 `localize` 全部改为中文；白底图质检的修复样例 `suggestedPrompt` 从要求英文改为要求中文（含中文修复示例短语）。此前（六）（七）的英文规则决策按用户要求回退；通义系图片模型对中文提示词理解良好（早期版本即中文直拼）。
- 验证：后端 Maven compile 通过；前端 tsc + vite build 通过。

## 2026-08-30（十一）：单图工作台不再预填默认描述

- 「02 文字描述 / 改写要求」打开时默认为空（placeholder 引导 + 选项 chips 点击插入），删除六个工具的预填默认描述常量 `DEFAULT_PROMPTS`；仅保留两类有明确任务语义的预填：质检「用此提示词修复」带入的修复样例、槽位「工作台」带入的原提示词。本地化改写要求随之改为必填。
- 验证：`tsc -b` + vite build 通过。

## 2026-08-30（十）：槽位图片双击放大预览 + 下载入口收敛到右上角按钮

- **双击放大**：生成工作台槽位图片双击打开全屏 lightbox（`ImageLightbox`：原图按 `86vw/78vh` 约束缩放居中，Esc / 点击遮罩 / 「关闭」按钮三种方式关闭，打开期间 body 滚动锁定），lightbox 内附「下载原图」与图片说明（类型 · 平台 · 尺寸）；**单击图片不再触发下载**。
- **下载按钮**：槽位右上角操作组新增「下载」（首位，经同源代理 `Content-Disposition: attachment` 下载，承接原点击图片下载行为），组内顺序：下载 / 工作台 / 重新生成 / 修改。
- 无障碍：图片容器（`role=button`）Enter 键从下载原图改为打开放大预览；tooltip 更新为「双击放大预览；右上角可下载原图」。
- 验证：`tsc -b` 通过；真实 E2E（Amazon 五图生成 + 白底图视觉质检通过）实测——双击打开 lightbox、几何正确（2048² 原图 448² 呈现、水平居中）、Esc 关闭、下载按钮触发下载事件，全部通过。

## 2026-08-30（九）：AI 详情页自动化升级为独立能力，覆盖各类工作台

- **新增独立端点 `POST /api/detail-page`**：商品名称/卖点（至少其一）+ 平台多选 + 语气 → 内部构建商品画像（失败降级纯文本）→ 复用流水线 AI 编排逐平台生成详情页（失败降级模板）；`generatedTypes` 传入已有生成图类型集合供 AI 引用配图。与五图流水线解耦，无需先跑五图即可输入卖点生成详情页；画像提示词抽为 `profilePrompt` 与流水线共用。
- **前端新增「AI 详情页」工作台整页**（侧栏工具直达，`DetailWorkbench`）：01 商品资料（预填当前任务名称/卖点）、02 平台规范与语气（四平台多选 + 三档语气，预填当前任务选择）、03 配图引用（自动带入生成工作台已完成槽位图；无图时输出纯文案模块并提示）；结果复用 `DetailPages` 渲染，支持「复制全部文案」，生成中有骨架加载反馈。
- `DetailPages` 渲染组件从 StudioView 抽出为共享 `DetailPageView.tsx`（生成工作台与详情页工作台共用，渲染逻辑不变）；`SideToolType` 扩展 `'详情页'`。
- 生成工作台流水线内的详情页展示保持不变（跑五图时仍自动附详情页）。
- 验证：后端 Maven compile 通过；前端 tsc + vite build 通过；端到端实测通过（重启后端进程加载新端点后：侧栏「AI 详情页」→ DetailWorkbench 填名称/卖点 → `POST /api/detail-page` 真实生成 → Amazon 版 7 模块详情页渲染、27 条卖点/FAQ、「复制全部文案」生效；无已生成图时正确输出纯文案模块结构）。注意：该端点要求后端进程在代码更新后重启，旧进程会 404。

## 2026-08-30（八）：单图工作台提示词回退中文

- 单图工作台「02 文字描述」全链路改回中文：六个工具的默认描述（`DEFAULT_PROMPTS`）与选项 chips 点击插入的短语统一为中文（原双语 zh/en 结构简化为纯中文短语，插入/移除分隔符兼容中文逗号）；回退（七）的「发送给图片模型的提示词统一英文」决策（按用户要求）。界面引导文案不变。

## 2026-08-30（七）：预置提示词全英文化 + 仓库整理

- 单图工作台六个工具的默认文字描述、后端本地化兜底指令全部改为英文（直接发送给图片模型的提示词统一英文）；界面引导文案（placeholder/合规提示条/分组标签）保留中文。
- 仓库整理：删除 `.trae-html-share-packages/`（插件分享 zip）、空目录 `.java-tmp/` 与 `apps/server/src/{agents,routes,services}/`、陈旧构建产物（`vite.config.js/.d.ts`、`*.tsbuildinfo`）、前端死代码 `runImagePipeline`；文档补齐质检自动重试与修复样例说明（CLAUDE 降级策略 / integration-guide QA 字段 / architecture 流程）。

## 2026-08-30（六）：质检修复样例与自动重试 + 平台审核规则前置 + 单图工作台体验优化

- **质检未通过给出修复提示词样例**：视觉质检 JSON 新增 `suggestedPrompt`（针对 issues 的英文修正提示词，符合平台规范）；质检摘要未通过卡展开展示样例，支持复制或「用此提示词修复」一键跳转对应单图工作台并预填。
- **未通过自动修复重试**：白底图质检未通过且有修复样例时，自动按样例重新生成一次并二次质检，二次结果为准（槽位实时更新）；重试仍不过则保留结果并提示到工作台继续调整。
- **生成提示词前置平台审核规则**：按（平台 × 图类）注入英文合规规则——Amazon 主图要求纯白 RGB 255/无阴影/无文字水印道具/无 logo/占 85% 等，TikTok 竖版优先、Temu 商品细节优先、Shopee 移动端清晰；实测两次白底图质检均一次通过（此前自行车样例曾未通过）。
- **画像去 markdown**：商品画像拼入生成提示词前剥离 `**` 加粗符。
- **单图工作台**：选项升级为双语（chip 显示中文，点击插入对应英文提示词短语，不再中文直拼）；「基于当前图修改」默认把当前图带入 01 源图（可更换），源图缺失时回退当前图。

## 2026-08-30（五）：白底图质检升级为视觉模型自动审核（内容理解与合规检测）

- **推翻旧结论**：Token Plan `/v1/models` 清单虽无 `vl` 字样模型，但实测文本档 **`qwen3.6-plus` / `qwen3.6-flash`（126 清单内）为多模态视觉模型**（OpenAI 嵌套 `image_url` 格式传图 + base64 data URL 均可用，`enable_thinking:false` 后 `content` 直接返回答案；图片宽高须 >10px）；`qwen3.7-max` 确认纯文本。注意：图片生成/编辑模型仍必须用扁平 `{type:"image", image:url}` 格式，视觉理解模型相反必须用嵌套格式。
- **白底图质检升级**：新增 `ModelRouterVisionClient`（视觉理解封装），流水线在白底图生成后自动调用 `qwen3.6-plus` 审核——白底纯净度、商品完整性、水印/二维码/敏感内容/品牌侵权检测，输出结构化 JSON；质检记录新增 `issues`（未通过项明细）与 `model`（审核模型）字段；调用或解析失败自动降级为原「人工复检提醒」。
- **E2E 验证**：完整流水线实测通过——视觉质检真实拦截了一张印有 `amazon` 字样的违规白底图（`passed=false`，issues 给出品牌侵权风险原因）。
- **模型目录**：`GET /api/models` 新增 `vision` 分组（清单内实测视觉可用模型，`qwen3.6-plus` 标记 verified）与真实 `visionAvailable` 判定（不再恒为 `false`）。
- **前端**：右栏「视觉模型 · 内容理解与合规检测」从琥珀降级卡片升级为可选质检模型选择器（网关无可用视觉模型时仍显示降级提示）；生成工作台质检摘要展示审核模型徽标与未通过项明细；创作页调用计划加入视觉质检次数。
- 配置：新增 `MODEL_ROUTER_VISION_MODEL`（默认 `qwen3.6-plus`，126 清单内具备视觉能力的 `qwen3.6-flash` 可替换）；`qwen3.7-plus` / `qwen3.8-max` / `qwen3.8-flash` 实测视觉可用但不在 126 清单内，不作选型（红线 6）。

## 2026-08-30（四）：单图工作台按工具特性差异化配置

- **侧栏高亮跟随当前工作台**：默认高亮「五图套图生成（主流程）」，点击任一工具（或从槽位「工作台」按钮进入）后高亮移动到对应工具项；回到创作/生成工作台后高亮回落主流程。
- 各工具工作台不再共用同一套配置，按自身特性区分：
  - **白底图**：光影 / 投影 / 构图三组选项 + 平台主图合规提示条（Amazon 纯白 RGB 规范、TikTok/Temu/Shopee 各自要点），默认画幅 1:1；
  - **场景图**：场景预设 / 光线时段 / 景深三组选项，默认画幅 3:2；
  - **模特图**：模特人设 / 景别 / 姿态三组选项，默认画幅 2:3；
  - **对比图**：版式 / 标注语言 / 对比维度三组选项 + 「AI 文字可能有小误差」人工复核提醒，默认画幅 3:2；
  - **尺寸图**：标注内容 / 标注风格 / 单位制三组选项 + 参数人工核对提醒，默认画幅 1:1；
  - **本地化**：目标市场（US/UK/欧洲/日本/东南亚）+ 改写要求，源图必选。
- 选项为提示词短语插拔（点击插入、再点移除），与文字描述实时联动。
- 修复：切换工具类型时组件未重挂载，导致画幅/描述残留上一工具状态（App 层补 key）。

## 2026-08-30（三）：侧栏工具直开单图工作台整页 + 图片点击下载

- **侧栏工具 → 单图工作台整页**：左侧「白底图生成 / 场景图生成 / 模特图生成 / 对比图生成 / 尺寸图生成」点击后打开对应类型的**全新整页工作台**（不再是弹窗抽屉），三区布局：01 参考素材（该图专属参考图，走图生图）、02 文字描述（以文字生成 / 基于当前图修改双模式 + 快捷风格 chips）、03 输出规格（1:1 / 3:2 / 2:3 画幅 + 调用模型展示）。
- 槽位联动：生成工作台每张完成图 hover 的「工作台」按钮同样跳转整页并带入该槽位当前图与提示词；工作台内生成后可「应用回槽位」（经 App 级 slotIndex/slotUpdate 双向同步，日志记录）。
- 独立模式：无任务时侧栏直开为独立生成工作台（按类型预填中文默认描述），结果仅提供下载。
- **图片点击 = 下载原图**：点击图片卡经同源代理（Content-Disposition: attachment）直接下载，不再打开工作台；详细修改入口收敛到侧栏与「工作台」按钮。
- 「图片本地化」整页：必选源图 + 改写要求 + 目标市场（US/UK/欧洲/日本/东南亚），走 `/api/images/localize`。
- 交互规格：旧 `ImageWorkbench` 抽屉组件删除，由整页 `ToolWorkbench` 取代。
- 端到端验证：独立模式（默认描述预填）、槽位带入（Amazon · 当前 1024x1024）、工作台生成→应用回槽位（日志确认）、本地化生成都已实测通过。

## 2026-08-30（二）：AI 详情页自动化 + 单图工作台 + 计时修复

- **AI 详情页自动化**：流水线新增 AI 编排步骤——文本模型按平台规范（Amazon A+ 模块化 / TikTok 竖版种草 / Temu 卖点直给 / Shopee 移动端简洁）组合商品画像、卖点与本次生成图类型，输出结构化详情页 JSON（6-8 个模块，每模块含配图引用 imageType / 正文 / bullets / FAQ / 合规提示）；前端详情页按「模块配图 + 文案」左右交替组合渲染，AI 失败自动降级模板并记录步骤。
- **单图工作台**：每张完成图新增「工作台」入口（点击图片卡或 hover「工作台」按钮），抽屉三区：01 参考素材（该图专属参考图上传/URL，走图生图）、02 文字描述（预填提示词 + 以文字生成/基于当前图修改双模式 + 快捷风格 chips）、03 输出规格（1:1 / 3:2 / 2:3 画幅）；生成结果当前图/新图对比，可应用回槽位、下载原图、下载画幅裁切图（新增同源代理 `GET /api/image-proxy` 供 canvas 裁切与下载）。
- **修复**：任务完成后状态条耗时仍走动——定时器在 done/fatal 停表并在完成时精确锁定。
- 实测说明：网关实测忽略 `size` 参数（传 1536×1024 仍返回 2048×2048），故画幅为裁切输出规格，工作台内如实标注。
- 端到端验证：AI 详情页（Amazon 版 7 模块 + 5 图组合）、计时锁定、工作台生成→应用槽位 全部通过；前后端构建通过。

## 2026-08-30（一）：参考图生成 + 模型可选 + SSE 过程流 + 单图重生成工作台

- 新增**商品参考图**：本地上传（转 base64 直传网关，实测 `data:image/...;base64` 可用且单次可传多张）+ 粘贴公网图片 URL，最多 6 张；有参考图时五图自动走图生图（默认 qwen-image-2.0），生成结果与商品外观保持一致。
- 新增 `GET /api/models`：网关模型清单按能力分组返回（文生图 wan2.7-image / wan2.7-image-pro，图生图 qwen-image-2.0 / qwen-image-2.0-pro，文本 16 个，音频 2 个；实测无 VL 视觉模型）。
- 新增 `POST /api/images/set/stream`：SSE 流式流水线，逐条推送 `log`（思考过程文字）/ `profile` / `image_start` / `image_done` / `image_fail` / `done` / `fatal`；前端生成过程实时可见，不再黑盒等待。
- 请求支持模型覆盖：`imageModel` / `editModel` / `textModel`（前端右栏「模型与调用」面板选择）；`TokenPlanChatModel` 支持按请求 `ChatOptions.model` 覆盖文本模型。
- `POST /api/images/single` 扩展三分支：`sourceUrl`（基于已生成图修改，图生图）/ `referenceImages`（参考图生成）/ 纯文生图，均带模型覆盖。
- 前端工作台重构：创作页三区布局（01 商品参考图 / 02 商品资料 / 03 模型面板右栏）；生成工作台（状态胶囊 + 完成计数 + 耗时、双平台图片槽位 pending/running/done/failed 四态、深色思考日志台、商品画像折叠、质检摘要与详情页草稿）；单图 hover 出「重新生成 / 修改」，提示词可编辑后重生成。
- 校验放宽：商品名称与参考图二选一必填；卖点在有参考图时可留空。
- 修复：切换 tab 重挂载 StudioView 导致任务重跑/流中断（现保持挂载仅隐藏）；商品画像 markdown 星号原样显示；质检行补充平台归属；右栏 select 溢出与下拉箭头。
- 实测验证：SSE 端到端 6/6 成功（66s）；单图重新生成端到端；失败路径（无效参考图域名）真实网关错误逐条落日志；impeccable detect.mjs 0 findings；前后端构建通过。
- 评审截图存 `.impeccable/review/`（desktop / desktop-studio / desktop-running / desktop-regen / desktop-failed / desktop-empty / mobile / mobile-studio）。

## 2026-08-29（一致性清理：删除无用代码与材料，对齐实际实现）

- 删除与项目方向不符的 `一键上架境外电商平台_横纵分析报告.md`（Listing API 上架调研，非 AI 图片生成交付物）。
- 前端删除无用功能：假画幅选择器（后端不支持指定尺寸）、侧边栏死导航（商品项目/任务中心）、旧步骤名死映射、未使用的 `regenerateImage`/`localizeImage` 客户端函数、参考图输入框（Token Plan 无 VL 模型，文本模型无法解析图片 URL）。
- `referenceImageUrl` 字段从前后端契约中移除（`ImagePipelineRequest` / `types.ts` / 表单）。
- `detailTone` 此前后端未使用，现真实生效：详情页草稿按「专业可信 / 种草转化 / 简洁高端」三档调整标题与文案基调（`fallbackPage`）。
- 质检文案对齐实际：前端「质检通过/未过」改为「待人工复检」，面板标题改为「生成确认」。
- 重写 `提交内容_方案概述与技术方案.md`：模型表、调用方式、工作流、后端栈全部对齐实测实现（旧版含 VL 模型、异步轮询、Node/Express 后端等过时内容）。
- 三个附加材料 HTML 同步更新：架构图（Express→Spring Boot、VL→实际模型、异步→同步）、业务流程图（模型名、质检→复检、尺寸说明）、产品原型（表单、流水线、模型清单）。
- README / CLAUDE / docs 清理残留：删「可选参考图」描述、流式输出补充实测说明、评审标准对照对齐实际模型与链路。
- 验证：前后端构建通过；`/api/images/set` 冒烟通过（五图 5/5，种草转化语气生效）。

## 2026-08-27（后端升级 + 图片生成跑通 + 清理）

- 后端升级为 **Java 25 + Spring Boot 4.0.6 + Spring AI 2.0.1**（Jackson 3：`tools.jackson.databind`；RestClient 超时改用 `Duration`），移除 Undertow，默认 Tomcat 11。
- 实测确定 Token Plan 图片调用方式并重写 `ModelRouterImageClient`：全部图片能力统一走 `POST /chat/completions` 多模态 content——文生图 `wan2.7-image-pro`（`[{type:"text"}]`），图生图 `qwen-image-2.0`（`[{type:"image",image:url},{type:"text"}]`）；`/images/generations`（url error）与异步调用（403）实测不可用，相关任务轮询接口一并移除。
- `/api/images/localize` 从异步任务改为**同步图生图编辑**，直接返回结果图；`GET /api/tasks/:taskId` 移除；`SingleImageRequest` 删除被忽略的 `size` 字段，响应 `size` 改为网关实际返回值。
- 端到端测试全部通过（真实 key）：`/api/health`、`/api/images/single`、`/api/images/localize`、`/api/images/set`（五图流水线 5/5 成功，返回真实图片 URL）。
- Token Plan 模型清单经 `GET /v1/models` 确认共 24 个且无 VL 视觉模型：选型收敛为 qwen3.7-max / wan2.7-image-pro / qwen-image-2.0，质检降级为人工复检提示。
- 清理旧版 Node/Express 后端遗留：`apps/server/src/*.ts`（routes/agents/services）、`package.json`、`tsconfig.json`、运行日志；后端为纯 Maven 工程。`.gitignore` 增加 `target/`、`.mvn-repo/`。
- 同步更新文档：`CLAUDE.md`（技术栈/目录/调用速查/路由）、`README.md`（模型表/附录 A 实测调用格式）、`docs/integration-guide.md`、`docs/architecture.md`、`docs/runbook.md`。

## 2026-08-27（早前）

- 前端完成工作室风格重构，补齐详情页展示与画幅交互。
- 后端模型 ID 切换为 Token Plan 支持的名称。
- Model Router 增加超时、错误和多响应格式处理。
- 前端 API 客户端增加单图重生成、本地化和异步任务查询。
- 删除未引用的 `DetailPagePanel.tsx` 与可再生构建产物。
- 清理 `apps/web/node_modules/.vite` Vite 缓存、`.impeccable/review/` 评审截图和 `.idea/` IDE 工作区配置；保留 Hook 配置、源码与参赛资料。
- 对齐默认 Token Plan 基地址、3100 后端端口，并记录图片接口 `url error` 已知限制。
- 后端迁移为 Java 21 + Spring Boot 3.4.1 + Spring AI 1.0.3；根命令改为 `npm run dev:server`（Maven）与 `npm run build`。
- Java API 保持 `/api/health`、`/api/images/set`、`/api/images/single`、`/api/images/localize`、`/api/tasks/:taskId` 契约。
- 迁移验证：Maven 构建成功；当前本机 JDK 25 启动时被 loopback selector 限制，Token Plan 图片端点直接返回 400 `url error`，尚未得到真实图片 URL。
