# Model Router API Key (issued after compute review; hardcoding into code/repo is forbidden)
MODEL_ROUTER_API_KEY=sk-xxx

# Model Router Base URL (no need to change in general)
MODEL_ROUTER_BASE_URL=https://model-router.edu-aliyun.com/v1

# Backend service port
PORT=3000
