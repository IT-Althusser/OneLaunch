# OneLaunch · 一键出海 — 初赛提交内容（表单填写版）

> 参赛场景：场景一「AI 智能上新」
> 选定需求方向：**AI 商品图片生成** — 自动生成白底图、场景图、模特图、对比图、尺寸图，适配多平台尺寸和风格要求
> 说明：以下各节与在线表单字段一一对应，可直接复制填写。

---

## 基本信息（参考）

- 团队名称：（填写）
- 队长信息：（填写）
- 参赛场景：场景一 AI 智能上新

## 方案名称

OneLaunch · 一键出海 — 跨境 AI 商品图片生成工作台

## 方案概述

### 解决的问题

商品图是跨境上新的第一大瓶颈：拍摄、修图、模特、场景布置依赖线下影棚与设计师，单 SKU 图片制作周期 1-3 天、成本高；Amazon 等平台对白底主图有严格规范（纯白背景、商品占比 85% 以上、无水印文字），不合规即拒登；Amazon、TikTok Shop、Temu、Shopee 各平台尺寸与风格要求不同，一套图要反复裁改。本方案用 AI 将商品图生产压缩为分钟级自动化流水线：卖家输入商品名称和卖点，系统自动完成「商品画像 → 提示词设计 → 五图生成 → 多平台风格适配 → 白底图视觉质检」，一键产出白底图、场景图、模特图、对比图、尺寸图的上架图包。

### 目标用户与业务痛点

目标用户：以 Amazon、TikTok Shop 为主的跨境电商卖家与铺货/精铺运营团队，尤其是没有专职摄影、设计团队的中小卖家和工厂型出海卖家。

业务痛点：

1. 图片制作贵：拍摄+修图+模特逐件外包，单 SKU 图片成本数百至上千元。
2. 图片制作慢：约拍、修图、返工迭代 1-3 天，拖慢整个上新周期，追不上热点与季节窗口。
3. 平台规范严：Amazon 白底主图有纯白背景、商品占比、无水印文字等硬性规范，人工制作不达标即被拒登。
4. 多平台重复劳动：Amazon、TikTok Shop、Temu、Shopee 尺寸与风格各异，一套图人工反复裁切调整。
5. 本地化审美差异：不同区域市场对背景场景、模特形象、画面风格偏好不同，通用图点击率低。

### 核心功能（5 项）

1. **五图套图一键生成**：输入商品名称与卖点，自动生成白底图、场景图、模特图、对比图、尺寸图五类上架必备图片。
2. **多平台风格自动适配**：按 Amazon / TikTok Shop / Temu / Shopee 风格要求输出对应版本，每个平台均生成完整五图，提示词按「平台 × 图类」差异化（每平台独有构图、氛围与合规规则），多平台图片组互不雷同。
3. **商品画像驱动生成**：大模型先从商品名称与卖点构建结构化商品画像（品类/外观/受众/场景），再为每类图片设计生成提示词，保证生成图与商品特征一致。
4. **白底图视觉质检**：qwen3.6-plus 视觉模型自动审核（背景纯度、商品完整性、水印文字与违规元素），未通过项列明原因，把平台拒登风险拦在上架之前。
5. **图片本地化扩展**：基于图生图编辑能力替换背景场景与画面风格，适配不同区域市场审美。

### 方案亮点

1. **分钟级出图**：从输入到五图套图+多平台版本全程自动化，单 SKU 图片生产从 1-3 天压缩至分钟级（实测五图流水线端到端跑通，返回真实图片 URL）。
2. **平台规范内置**：白底规范、平台风格要求内置于提示词模板，提示词按平台与图类自动编排。
3. **画像先生成**：先构建商品画像再驱动五图生成，避免"AI 图与商品不符"的行业通病。
4. **全同步链路**：Token Plan 网关统一走 `/chat/completions` 多模态调用（文生图 + 图生图），同步返回结果，链路简单可靠、易排障。
5. **工程现代化**：Java 25 + Spring Boot 4 + Spring AI 2 后端，模型 ID 集中配置，新增模型只改环境变量。

### 预期效果

- 效率：单 SKU 图片生产周期从 1-3 天压缩至 5 分钟内，支撑分钟级上新。
- 成本：图片制作外包成本下降 80% 以上，无摄影/设计团队也能上新。
- 质量：白底主图视觉质检自动拦截违规图，规范符合率提升，拒登率下降。
- 转化：按平台风格定制的套图提升点击率与转化率（可 A/B 验证）。
- 可扩展：平台风格模板与图片类型提示词集中管理，新平台、新图类低成本接入。

## 技术方案

### 拟使用的模型或能力（阿里云百炼 · Model Router API）

统一通过 Model Router API 调用（Base URL：`https://token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`；认证：`Authorization: Bearer <api-key>`），所选模型均经 Token Plan `GET /v1/models` 实测确认可用：

| 环节 | 模型 | 用途 |
|---|---|---|
| 商品画像 / 提示词设计 | qwen3.7-max | 从商品名称与卖点构建结构化商品画像，驱动五图提示词 |
| 五图生成 | wan2.7-image-pro | 白底图/场景图/模特图/对比图/尺寸图生成（同步调用） |
| 图片本地化替换 | qwen-image-2.0 | 背景与画面风格编辑（图生图，同步调用） |
| 白底图质检 | qwen3.6-plus | 视觉模型自动质检（内容理解与合规检测：白底合规/商品完整/水印与违规元素） |

调用要点（Token Plan 实测验证）：各项能力统一走 OpenAI 兼容的 `/v1/chat/completions`，多模态 content 数组按模型类型区分——文生图传 `[{type:"text"}]`，图生图传 `[{type:"image", image:url}, {type:"text"}]`，视觉理解传 OpenAI 嵌套格式 `[{type:"image_url", image_url:{url}}, {type:"text"}]`，同步返回。`/v1/images/generations` 与异步任务接口在该网关不可用（已实测）。

### AI Agent 或工作流

采用「1 个编排服务 + 4 项模型能力」的流水线架构（Spring Boot 单服务编排，无需独立 Agent 框架）：

- **编排服务（ImagePipelineService）**：解析用户输入，按流水线顺序调度模型能力，汇总步骤状态与结果，处理降级与容错。
- **商品画像**：qwen3.7-max 解析商品名称与卖点，输出结构化商品画像（品类/外观/人群/场景），作为全部生成提示词的统一事实源。
- **五图提示词与生成**：按图类模板（白底/场景/模特/对比/尺寸）+ 平台风格要求 + 商品画像组装提示词，wan2.7-image-pro 逐类生成。
- **本地化编辑**：qwen-image-2.0 图生图，输入源图 + 目标市场指令，同步返回替换后的图。
- **视觉质检**：qwen3.6-plus 对白底图做内容理解与合规检测（白底纯净度、商品完整性、水印/二维码/敏感内容/品牌侵权），输出结构化结论。

工作流（5 步）：

1. 输入：商品名称、核心卖点、目标平台（多选）、详情页语气（专业可信/种草转化/简洁高端）。
2. 商品画像：qwen3.7-max 构建结构化商品画像（失败时降级为纯文本拼接，流水线不中断）。
3. 五图生成：wan2.7-image-pro 按图类提示词逐类生成（每平台全五图，提示词按平台差异化）。
4. 白底图视觉质检：qwen3.6-plus 自动审核（背景纯度/商品完整/水印文字/违规元素），未通过项列明原因（质检失败时降级为人工复检提示）。
5. 输出：多平台上架图包 + 详情页草稿（含步骤状态、商品画像、质检结论）。

### 数据处理方式

- 输入数据：商品名称、卖点文本、目标平台多选、详情页语气。
- 结构化商品画像：大模型将商品名称+卖点转成画像文本（品类、外观特征、目标人群、使用场景），作为全部生成提示词的统一事实源。
- 结果数据：生成图 URL、类型、平台、尺寸、质检结论（通过/未通过 + 问题明细）结构化返回；后续版本入对象存储与数据库做版本化管理。
- 容错策略：任一步骤失败记录到步骤状态并继续，画像失败降级为纯文本拼接，单图失败不影响其余图类。

### 前后端及其他技术组件

- 前端：React 18 + TypeScript + Vite + Tailwind CSS；工作台式界面（左侧商品信息+平台多选表单，中部流水线步骤状态+分平台套图网格+质检结论标记，右侧模型编排与平台规范速览）。
- 后端：Java 25 + Spring Boot 4.0.6 + Spring AI 2.0.1（Maven）；流水线编排、REST API（`/api/images/set`、`/api/images/single`、`/api/images/localize`）。
- 模型调用层：文本走 Spring AI ChatClient（自定义 TokenPlanChatModel），图片走 ModelRouterImageClient，视觉质检走 ModelRouterVisionClient（均 `/chat/completions`），Bearer Token 认证走环境变量，带超时与错误透传。
- 部署与协作：本地 dev 双服务（前端 5173 / 后端 3100，Vite 代理 `/api`）；代码仓库已上传 GitHub。
